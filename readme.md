## Tourism Company Test

API key needed for the Open Weather Map to work.

It is set in the `.env.example` as a `OPEN_WEATHER_API_KEY` variable.

I will leave my API key for a few days, so you don't have to register.